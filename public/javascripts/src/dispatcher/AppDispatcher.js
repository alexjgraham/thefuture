/**
 * @flow
 * AppDispatcher.js
 *
 * This is the engine that runs the business
 * logic of the entire application.
 * Will setup more things when we have more views and components
 */

import {Dispatcher} from "flux"
export default new Dispatcher()
